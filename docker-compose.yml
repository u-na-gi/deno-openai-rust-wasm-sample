version: '3.9'
services:
  aimaid:
    image: aimaid:latest
    build: 
      context: .
      dockerfile: ./dockerfile.dev
    volumes:
      - ./:/app
      - ~/.gitconfig:/root/.gitconfig
      - ~/.ssh:/root/.ssh
    tty: true
    env_file:
      - .env.local

